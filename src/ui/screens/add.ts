export function Add(root: HTMLElement) {
  root.innerHTML = `
    <form class="space-y-4">
      <div class="text-butter/90 font-medium">Add Activity (placeholder)</div>
      <p class="text-sm opacity-80">We’ll wire the real form once we lock page design and fields.</p>
    </form>
  `
}
